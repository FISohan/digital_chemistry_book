export interface INavList {
    name: string;
    link: string;
}

export interface IIntro{
    chapterName?: string;
    chapterTitle?: string;
    chapterTitleEn?: string;
    photoUrl?: string;
    title?: string;
}

export interface ISlider{
    image: string;
}
